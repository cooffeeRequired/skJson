package cz.coffee.skriptgson.skript;

@SuppressWarnings({"unused", "DanglingJavadoc"})
public class TODO {
    /**
     * ? FINISHED 1. change ExprNewJson.class to -> [a] [new] json from [(text|string|request)] %object%
     * ! TODO 2. Fix ExprJsonListToJsonElement.
     * ? FINISHED 3. Add ExprCountOfJson.class -> [a] count of [key] %string% in %jsonelement%
     * ! TODO 4. Add ExprJsonRegexMatch.class -> [a] (last|first|%integer%) regex-match %string% in [the] %jsonelement%
     * ? FINISHED 5. Add CondJsonHas -> %jsonelement% has (1¦(:key|:value) %-string/integer/boolean%|2¦(:keys|:values) %-objects%) ! FINISHED
     */
}
