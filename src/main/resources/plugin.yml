name: SkJson
version: '${version}'
main: cz.coffeerequired.SkJson
api-version: '1.21'
revision-version: '${rev}'
depend:
  - Skript
soft-depend:
  - NBT-API
prefix: SkJson
author: CoffeeRequired
description: '&aDescription: &fSkript JSON library'
website: 'https://skjson.coffeerequired.info'
loadbefore:
  - skript-reflect
  - SkBee
commands:
  skjson:
    description: basic skjson
    usage: /skjson
permissions:
  skjson.use:
    default: op
    description: Allows you to use skjson